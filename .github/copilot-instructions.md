Before answering, planning, or generating code, read `CLAUDE.md` from the repository root and treat it as the project ground-rules source of truth.

If `CLAUDE.md` conflicts with higher-priority instructions (system/developer/user), follow instruction hierarchy; otherwise prefer `CLAUDE.md` for project-specific decisions.
