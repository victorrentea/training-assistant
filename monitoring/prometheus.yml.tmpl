global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "workshop"
    scheme: https
    metrics_path: /metrics
    basic_auth:
      username: "${HOST_USERNAME}"
      password: "${HOST_PASSWORD}"
    static_configs:
      - targets: ["interact.victorrentea.ro"]
