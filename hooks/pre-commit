#!/bin/sh
# Stamp work hours from git history before each commit
SCRIPT_DIR="$(cd "$(dirname "$0")/.." && pwd)"
if [ -x "$SCRIPT_DIR/stamp-work-hours.sh" ]; then
  "$SCRIPT_DIR/stamp-work-hours.sh" >/dev/null 2>&1
  git add -f "$SCRIPT_DIR/static/work-hours.js" 2>/dev/null
fi
